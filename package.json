{
	"scripts": {
		"bootstrap": "lerna run bootstrap --parallel",
		"start": "lerna run start --parallel",
		"update": "lerna run update --parallel",
		"watch": "lerna run watch --scope=todo --parallel"
	},
	"devDependencies": {
		"lerna": "^2.11.0"
	}
}
